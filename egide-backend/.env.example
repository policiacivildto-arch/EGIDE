# Django Settings
DEBUG=True
SECRET_KEY=sua-chave-secreta-muito-segura-aqui-mude-em-producao
ALLOWED_HOSTS=localhost,127.0.0.1,10.18.200.78

# Database (opcional em dev | obrigatório em produção Railway)
# Se DATABASE_URL não estiver definida, o sistema usa SQLite local.
# Exemplo Supabase (pooler):
# DATABASE_URL=postgresql://postgres.<project-ref>:<password>@aws-0-sa-east-1.pooler.supabase.com:6543/postgres
DATABASE_URL=

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://10.18.200.78:3000

# Email (opcional)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=seu_email@gmail.com
EMAIL_HOST_PASSWORD=sua_senha
EMAIL_TIMEOUT=10
PASSWORD_RESET_TOKEN_EXPIRY_MINUTES=15
